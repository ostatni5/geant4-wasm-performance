# Notes

## Perfomace issue

On mac os problem was that python was in x86 version that was emualted by rosetta.
To fix it I had to install python in arm version.

## Module compilation

To reduce ram usage I had to compile in browser and then instantiate module in web worker.

firefox mniejs zuzycie ramu gdy linowa inizlaicjiacja
